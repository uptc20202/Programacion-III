package test;

import model.Node;

public class Testinfo {

	/*public Node findMinimum() {
		if(root  == null) {
			return null;
		}
		
		Node work = root;
		
		while(work.getLeft() != null) {
			work = work.getLeft();
		}
		
		return work;
	}
	
	public Node fintMax() {
		if(root  == null) {
			return null;
		}
		
		Node work = root;
		
		while(work.getRight() != null) {
			work = work.getRight();
		}
		
		return work;
	}*/
}
